# Modifications

- An AI agent should use for for converting image to the text.
- Original text should in the same level with the image. Also source text should possible to zoom. Then user can compare the image and source text, and decide the image to text convert is correct or not.
- Exact letter transliteration and Your Translation also in same level. Then user can compare his translation. These fields no need zoom.
- Exact letter transliteration should generated by the AI agent. It should possible to edit. When it edit the source text also need to update. As example the source text is showing as मात् . Then Exact letter transliteration is showing it as මාත්. The user find the issue with the source by comparing source image then he is updating මාත් as the මාතා. Then Source text also need to update as माता.
- Also user should possible to edit source text then Then Exact letter transliteration should change with that.
