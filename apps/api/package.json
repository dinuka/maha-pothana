{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,celery --prefix-colors blue,green \".venv/bin/uvicorn app.main:app --reload --port 8000\" \".venv/bin/celery -A app.tasks.celery_app worker --loglevel=info\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
